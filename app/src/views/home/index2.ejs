<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>B/O App. - WashSwat</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="/js/home/getitems.js"></script>
</head>
<body>

    <div class="container-fluid">
        <div class="row flex-nowrap text-center">
            <% for(var i=0; i < data.length; i++) { %>
            <div class="col-lg-2 fs-4">
                <!-- 카테고리 이름 -->
                <div>
                    <p><%= data[i].categoryName %></p>
                </div>
                <!-- 카테고리별 주문 개수 -->
                <div>
                    <p><%= data[i].orderCount %></p>
                </div>
                <div>
                    <div class="row">
                        <% for(let j=0; j < data[i].orders.length; j++) { %>
                            <div class="accordion" id="accordionExample">
                                <div class="accordion-item">
                                  <h2 class="accordion-header" id="heading-<%= data[i].orders[j].id %>">
                                    <button id="<%= data[i].orders[j].id %>" class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-<%= data[i].orders[j].id %>" aria-expanded="true" aria-controls="collapse-<%= data[i].orders[j].id %>" onclick="showItems('<%= data[i].orders[j].id %>');">
                                        <%= data[i].orders[j].id %>
                                    </button>
                                  </h2>
                                  <div id="collapse-<%= data[i].orders[j].id %>" class="accordion-collapse collapse show" aria-labelledby="heading-<%= data[i].orders[j].id %>" data-bs-parent="#accordionExample">
                                    <div class="accordion-body">
                                        <h6></h6>
                                    </div>
                                  </div>
                                </div>
                            </div>
                        <!-- <div class="col">
                            <p>
                                <button id="<%= data[i].orders[j].id %>" class="btn btn-primary fs-6" type="submit" onclick="showItems('<%= data[i].orders[j].id %>');">
                                    <%= data[i].orders[j].id %>
                                </button>
                                <div id="items_<%= data[i].orders[j].id %>" extend="false">
                                    <h6></h6>
                                </div>
                            </p>
                        </div> -->
                        <% } %>
                    </div>
                </div>
            </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>